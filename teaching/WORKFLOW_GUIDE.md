# 🔄 Workflow 情境指南

根據不同的工作情境，使用對應的 Workflow 組合。

---

## 📅 每日開發流程

### 🌅 一天的開始

```
/sync    → 同步最新程式碼
/run     → 啟動本地預覽
```

### ✏️ 開發中（修改程式碼）

```
（修改程式碼...）
/commit  → 儲存進度點 (可多次)
```

### 🌙 一天的結束

```
/doc-check → 確認文件是否需要更新
/commit    → 提交最後變更
/deploy    → 推送到 GitHub
```

---

## 🎯 特定情境

### 🆕 新增旅程頁面

```
/new-trip  → 建立新旅程模板
（編輯內容...）
/commit    → 提交
/deploy    → 上線
```

### 🔍 定期健檢（每週/每月）

```
/review    → AI 架構審查
（根據報告修復問題...）
/commit    → 提交修復
/deploy    → 推送
```

### 🐛 緊急修 Bug

```
/run       → 啟動預覽
（修復問題...）
/commit    → 提交修復
/deploy    → 立即上線
```

### 🧹 程式碼整理

```
/format    → 格式化程式碼
/commit    → 提交
```

### 🔀 合併分支

```
/merge [branch_name]  → 合併指定分支
/deploy               → 推送
```

---

## 📋 Workflow 速查表

| 指令         | 功能        | 何時用       |
| ------------ | ----------- | ------------ |
| `/sync`      | 拉取 + 推送 | 開始工作前   |
| `/run`       | 啟動預覽    | 需要看畫面時 |
| `/commit`    | 提交變更    | 完成一個段落 |
| `/deploy`    | 推送上線    | 結束工作時   |
| `/doc-check` | 文件檢查    | 提交前       |
| `/review`    | 架構審查    | 定期健檢     |
| `/new-trip`  | 新旅程模板  | 規劃新行程   |
| `/format`    | 格式化      | 程式碼亂掉時 |
| `/merge`     | 合併分支    | 多分支開發時 |

---

## 💡 記憶口訣

> **開始先 Sync，結束要 Deploy**
> **改完就 Commit，上線前 Doc-check**
